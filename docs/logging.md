# Structured Logging

This page has moved to [configuration/logging.md](configuration/logging.md), which is the
maintained version of this guide. The options it described (`--log-rotate`, `--log-format`) do not
exist; use `--log-file`, `--log-json` and the verbosity flags documented there.
