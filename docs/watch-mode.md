# Watch Mode

This page has moved to [configuration/watch-mode.md](configuration/watch-mode.md), which is the
maintained version of this guide. The options it described (`--debounce`, `--include`, `--exclude`)
do not exist; use `--watch-debounce`, `--watch-clear` and the `watch` configuration section
documented there.
