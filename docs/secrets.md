# Secrets

This page has moved to [configuration/secrets.md](configuration/secrets.md), which is the
maintained version of this guide (AES-256-GCM store in `~/.pdk`, masking rules, `pdk secret`
commands).
