# Variables

This page has moved to [configuration/variables.md](configuration/variables.md), which is the
maintained version of this guide (variable sources, `PDK_VAR_*` / `PDK_SECRET_*` import rules,
`${VAR}` expansion and how variables reach steps).
